import type { Metadata } from "next";

// Centralize SEO defaults for the main site surface.
export const siteMetadata: Metadata = {
  // TODO: set title, description, canonical URLs, and social previews.
};
